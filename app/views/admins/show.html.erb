<%= link_to "Back to locations", admin_path %>
<%= link_to "Logout", logout_path %>

<div id="admin-tables">
  <table class="admin-table">
    <tr><td colspan="9">PRIORITIES (flagged by users)</tr>
    <%= render "show_headers" %>

    <% @admin.priorities.each do |post| %>
      <%= render partial: "show_row_post", locals: {location: @admin.location, post: post} %>
    <% end %>
  </table>

  <table class="admin-table">
    <tr><td colspan="9">ALL POSTS</td></tr>
    <%= render "show_headers" %>

    <% @admin.posts.each do |post| %>
      <%= render partial: "show_row_post", locals: {location: @admin.location, post: post} %>
    <% end %>

  </table>
</div>